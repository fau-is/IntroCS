#include <stdio.h>
#include <string.h>
#include <ctype.h>
#include <cs50.h>

int main(void)
{
    while (true)
    {
        char c = get_char("C: ");

        // Let's write a program that returns the alphabetical index
        // of a letter: e.g. A = 0, B = 1, C = 2, ...
    }
}
