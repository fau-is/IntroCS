#include <stdio.h>

// Define count of rows and length of rows
#define ROWS 4
#define ROW_LEN 9

// Seats have name, vote count, eliminated status
typedef struct
{
    string name;
    int votes;
    bool eliminated;
}
candidate;

int main(void)
{

}